<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Blacktasks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="SpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.Blacktasks" parent="Base.Theme.Blacktasks" />

    <!-- aplicacion | tema | modo dia -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/colorPrimaryDay</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDarkDay</item>
        <item name="colorSecondary">@color/colorAccentDay</item>
        <!-- Fondo general -->
        <item name="android:windowBackground">@color/backgroundLight</item>
        <item name="android:textColor">@color/textPrimaryLight</item>
    </style>
</resources>